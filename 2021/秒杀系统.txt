1、秒杀系统独立部署，和现有系统隔离，避免互相影响；
2、秒杀页面使用独立页面，不使用原来的商品页面，同上，避免互相影响；
3、CDN、带宽；
4、前端根据一定算法过滤掉绝大部分用户，不让其进入后端服务；
5、对应秒杀器：验证码等
6、扣减库存：update t_goods_storage set count = count - #{value} where count >= #{value}
7、userID限制访问频率；
8、同一个item的查询使用页面缓存，比如车次等信息；
9、对于写请求，直接入队列，分批处理；
10、对于同一个IP访问次数过多，可以通过验证码过滤拦截；
11、对某些账号，总结历史轨迹信息，设置账号黑名单；
